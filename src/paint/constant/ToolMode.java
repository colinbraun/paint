package paint.constant;

/**
 * Enum to hold tool mode constants
 */
public enum ToolMode {
    LINE(),
    ELLIPSE(),
    CIRCLE(),
    RECTANGLE(),
    SQUARE(),
    PENCIL(),
    TEXT(),
    POLYGON(),
    ERASER(),
    SELECT(),
    COLOR_PICKER()
}
